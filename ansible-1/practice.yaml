- name: using variables to print the message
  hosts: web
  # vars:
  #    course: "devops with aws"
  #    duration: 120hrs
  # vars_files:
  # - vars.yaml 
  vars_prompt:
  - name: course 
    prompt: please enter the course name
    private: true
  - name: duration 
    prompt: please enter the duration
    private: true
  tasks:
  - name: print the info
    ansible.builtin.debug:
      msg: " hi i am learing {{ course }}, duration is {{ duration }}"

  
    
