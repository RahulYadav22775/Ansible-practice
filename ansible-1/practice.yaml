- name: print the msg-1
  hosts: web
  tasks:
  - name:  print the msg-1
    ansible.builtin.debug:
     msg : "hi  i am learning devops with aws"

- name:  print the msg-2
  hosts: web
  tasks:
  - name:  print the msg-2
    ansible.builtin.debug:
     msg : "hi  i am learning DEVOPS"

    
